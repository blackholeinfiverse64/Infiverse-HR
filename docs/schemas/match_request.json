{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MatchRequest",
  "description": "Schema for match requests from Gateway to Agent or for recording match requests in audit logs.",
  "type": "object",
  "properties": {
    "job_id": { "type": "string", "description": "Canonical job identifier (DB ObjectId or UUID)." },
    "job_requirements": { "type": "string", "description": "Normalized plain-text job requirements or extracted skills." },
    "candidate_ids": { "type": "array", "items": { "type": "string" }, "description": "Optional list of candidate ids to consider." },
    "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 10 },
    "correlation_id": { "type": "string", "format": "uuid", "description": "Correlation id propagated across systems." },
    "requested_by": { "type": "string", "description": "Actor requesting the match (e.g., recruiter|client|system)." },
    "timestamp": { "type": "string", "format": "date-time" }
  },
  "required": ["job_id"],
  "additionalProperties": false,
  "examples": [
    {
      "job_id": "679a1b2c3d4e5f6789012345",
      "job_requirements": "5+ years Python, FastAPI, MongoDB, API design",
      "limit": 5,
      "correlation_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
      "requested_by": "recruiter",
      "timestamp": "2026-05-30T10:15:30Z"
    }
  ]
}
