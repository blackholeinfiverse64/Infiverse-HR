# Handover Folder Audit — 2026-08-10

**Captured by:** Documentation-plan audit (Claude session, sandboxed — no VM/Atlas network access)
**Method:** Static code verification (`py_compile`, `grep`, direct file reads) + a local gateway boot test against the real `backend/.env`, run against the codebase in this same zip. No secret values were printed at any point; only variable/file names and non-secret URLs.
**Scope of this pass:** Audited `handover/` for correctness against Handover.md's requirements and against the code itself — not a re-run of the live VM checks in EVD-001/EVD-002, which already cover that from outside this sandbox.

---

## What was checked and confirmed accurate

| Claim (source) | Check | Result |
|---|---|---|
| KI-001 fix applied (`_as_utc_aware`, client login) | Read `backend/services/gateway/app/main.py` directly | ✅ Confirmed present and correctly used at the account-lock comparison |
| Gateway health/root/docs/metrics shape (EVD-001/002) | Local boot: `pip install -r services/gateway/requirements.txt`; `uvicorn app.main:app --port 8000`; curl `/health`, `/`, `/docs`, `/metrics` | ✅ Matches EVD-001/002 response shapes exactly (service name, version `4.2.0`) |
| All 3 services' entrypoints are syntactically valid | `python -m py_compile` on gateway/agent/langgraph main files | ✅ Clean |
| Existing test suite runs | `pytest tests/gateway/test_gateway_imports.py test_workforce_lifecycle.py` | ✅ 5/5 passed |

## What was found incorrect or missing, and fixed in this pass

| Issue | File(s) | Fix applied |
|---|---|---|
| Password derivation pattern documented (violates Handover.md's "do not expose secret/password values") | `11_CREDENTIALS_REGISTER.md`, `12_DEMO_SESSION.md` | Pattern description removed; replaced with "stored in owner's secure channel" |
| "Full BHIV ecosystem" scope claim conflicts with owner's stated scope (gateway/agent/langgraph/frontend only; portals archived) | `IMPLEMENTATION_PLAN.md`, `00_INDEX.md`, `01_EXECUTIVE_OVERVIEW.md`, `09_DEPENDENCY_MAP.md` | Narrowed to match; partner ecosystem repos reframed as "Sampada's calling code only, not in this repo" |
| Secrets-risk table incomplete/unverified, and inconsistent between `IMPLEMENTATION_PLAN.md` (3 rows) and `11_CREDENTIALS_REGISTER.md` (4 rows) | Both files | Verified directly (see below); synced to the same 4 rows with a "Verified" column |
| Gateway hardcodes a dead Render URL in its own root endpoint (new finding — KI-005) | `backend/services/gateway/app/main.py` line ~803, `07_KNOWN_ISSUES.md` | Logged as KI-005 with full write-up; one-line code fix applied (now points at the VM production URL) |

## New verification performed (not previously in this folder)

```
$ grep -rn "prod_api_key" evidence/entry-points/curl-examples.sh backend/tools/utilities/verify_changes.py
# same literal key confirmed in both files (value not reproduced here)

$ grep -rl "prod_api_key" backend/tests/ | wc -l
27

$ grep -rn "ltg0\|l0xp" backend/services/gateway/
backend/services/gateway/app/main.py:803:  "production_url": "https://bhiv-hr-gateway-ltg0.onrender.com"
# l0xp does not appear anywhere in the codebase — it's only known from the live Render dashboard / EVD-002's external check
```

This confirms KI-003's scope precisely (29 files, one key) and KI-005's root cause (a stale literal, not a config/env issue).

## Not verified this pass (needs real network access, outside this sandbox)

- Live VM / Render / Atlas connectivity — already covered by EVD-001/EVD-002 from a machine with real access
- Whether the `prod_api_key_...` value (KI-003) still carries live production privileges
- `backend/services/portal/auth_manager.py` — deprioritized, `portal` is archived per scope decision
